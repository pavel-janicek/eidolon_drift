From root run
`PYTHONPATH=$(pwd) python3 -m eidolon.io.input_handler --map-test` to test mapping of controller

or

`PYTHONPATH=$(pwd) python3 -m eidolon.io.input_handler --monitor"` to check what controller is connected